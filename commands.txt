
pnpm install --config.platform=linux --config.architecture=x64 --force
docker compose up --build -d; docker compose logs -f;

when commit, need to install devDependencies otherwise husky and commitlint will not work
pnpm install --only=dev