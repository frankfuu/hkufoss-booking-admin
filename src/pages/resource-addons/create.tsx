import { Create, SaveButton } from "@refinedev/mui";
import { useForm } from "@refinedev/react-hook-form";
import { useTranslation } from "react-i18next";
import { Typography } from "@mui/material";
import EditCreateFunders from "./edit-create-resource-addons";
import EditCreateActivityNatures from "./edit-create-resource-addons";
import EditCreateActivityTypes from "./edit-create-resource-addons";
import EditCreateResourceAddons from "./edit-create-resource-addons";

export const ResourceAddonsCreate = () => {
  const { t } = useTranslation();

  const {
    saveButtonProps,
    refineCore: { formLoading },
    register,
    control,
    formState: { errors },
  } = useForm();

  return (
    <Create
      title={<Typography variant="h5">{t("create") + " " + t("Resource Addons")}</Typography>}
      isLoading={formLoading}
      saveButtonProps={saveButtonProps}
      footerButtons={({ defaultButtons }) => (
        <>
          <SaveButton variant="contained" {...saveButtonProps}>
            {t("save")}
          </SaveButton>
        </>
      )}
    >
      <EditCreateResourceAddons {...{ register, errors, control, action: "create" }} />
    </Create>
  );
};
